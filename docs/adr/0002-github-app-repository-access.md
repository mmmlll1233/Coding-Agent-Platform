---
status: accepted
---

# Access repositories through a GitHub App

The platform will access only repositories authorized through a GitHub App and will use short-lived installation credentials instead of accepting personal access tokens in Work Requests. This trades initial GitHub App setup for narrower permissions, revocable repository access, and a credential model suitable for unattended cloud execution.

